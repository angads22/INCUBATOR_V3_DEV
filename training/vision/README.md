# Vision Training Workspace

- This directory is for training pipeline code and metadata only.
- Training is performed off-device (cloud workstation, desktop, or CI runner).
- Exported runtime models are copied to `models/vision/` on target devices later.
