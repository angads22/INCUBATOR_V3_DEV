# LLM Training Workspace

- This directory is for LLM training/fine-tuning pipeline code and metadata only.
- Training is performed off-device (not on the incubator runtime device).
- Exported runtime models are copied to `models/llm/` on target devices later.
