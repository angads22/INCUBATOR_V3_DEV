# Vision Runtime Models

- Runtime vision model files for on-device inference are placed in this directory.
- Do not commit model weights or large binaries to git.
- Train and export models off-device, then copy optimized runtime artifacts here.
